# Cursor — one close verb, phases 3–14 checked

Claude is out of credit. Cursor owns the plan and the instrument.

## This pass

The first live checkbox was two promises on the close: the thesis and a button
that sounded like GitHub. Connect Repository is now one verb. It is an alpha
invite, not a login.

- `connectCta` → `/#waitlist`, label `Connect Repository`, hint `Alpha`.
- Header, hero, close, and form share that object.
- Bridge under the thesis: “The alpha is an invite to connect a repository.
  Not a login.”
- “What the alpha will not do” stays.

Then each product phase was walked on the live page and marked only with
evidence. No Phase 15. No Needle. No WebDesigner intake.

## Verified

- `tsc`, `eslint .`, fixtures 211/0, `check-beats` 12/0, `next build` 29 pages.
- Desktop 1440×900: canvas, animated hero, 17 sections, 12 beats.
- Header Connect Repository → `#waitlist`, form top 64.
- Form `carlos@example.com`: “Noted locally”, nothing transmitted.
- Trace Failure enables Reset. Sideways live region. Replacement demo only.
- Mobile 390×844 hamburger lists the same IA.
- `?helix=none`: no canvas, static hero, story still in the document.
- `/explore` `/lineage` `/blast` `/research` `/docs` `/family/keylit` 200.
- No OAuth URL.

## Open

The next real instrument defect. Do not restart Phases 3–14.
